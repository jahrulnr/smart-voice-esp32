# UI Feature

This folder contains the user interface feature, including display and notifications.

- `display.h/.cpp`: Manages the U8g2 display for visual output.
- `notifications.h/.cpp`: Handles notifications using the esp32-notification library.