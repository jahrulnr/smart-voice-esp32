# Web UI targets
up: ## Start web UI for MQTT monitoring
	@echo "Starting MQTT Web UI..."
	@cd web && docker-compose up -d

down: ## Stop web UI
	@echo "Stopping MQTT Web UI..."
	@cd web && docker-compose down